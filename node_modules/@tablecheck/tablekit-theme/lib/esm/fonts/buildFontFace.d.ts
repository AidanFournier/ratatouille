import { SerializedStyles } from '@emotion/utils';
import { ReactNode } from 'react';
import { FontWeight } from './weights';
export declare type FontType = [ReactNode[], SerializedStyles];
export declare function getCdnPath(fontFolder: string, fontFile: string): string;
export declare function buildPreloadLink(href: string): ReactNode;
export declare function buildFontFace({ name, weight, unicodeRange, isItalic, woff, woff2 }: {
    isItalic?: boolean;
    name: string;
    unicodeRange?: string;
    weight: FontWeight;
    woff: string;
    woff2: string;
}): SerializedStyles;
