import { MediaQuery, VariantProps } from '@tablecheck/tablekit-utils';
import { HTMLAttributes } from 'react';
import { SpinnerSize } from './types';
export declare type PropsType = VariantProps<HTMLAttributes<HTMLSpanElement> & {
    /** custom className to put on the Outer div element */
    className?: string;
    /** size of the spinner */
    size?: MediaQuery<SpinnerSize>;
}>;
export declare function Spinner({ className, size, ...restProps }: PropsType): JSX.Element;
