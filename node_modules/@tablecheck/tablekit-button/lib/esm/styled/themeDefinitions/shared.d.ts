import { ThemeOnlyProps } from '@tablecheck/tablekit-utils';
import { ColorTheme } from '../../themes';
import { Color } from '../../types';
export declare type VariantProps = ThemeOnlyProps & {
    color: Color;
};
export declare const borderWidth = 2;
export declare function resolveColor(targetValue: keyof ColorTheme, props: ThemeOnlyProps & {
    color: Color;
}): string;
