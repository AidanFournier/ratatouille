import { Theme } from '@emotion/react';
import { BaseButtonProps, DerivedButtonProps } from '../types';
export declare const isVerticalButton: ({ appearance }: BaseButtonProps & {
    disabled?: boolean;
}) => boolean;
export declare const getButtonProps: (props: BaseButtonProps & {
    disabled?: boolean;
}, theme: Theme) => DerivedButtonProps;
