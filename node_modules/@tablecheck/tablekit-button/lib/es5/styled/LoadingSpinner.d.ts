import { MediaQuery } from '@tablecheck/tablekit-utils';
import { ButtonSize } from '../types';
declare type PropsType = {
    size?: MediaQuery<ButtonSize>;
};
export declare const LoadingSpinner: ({ size: buttonSize }: PropsType) => JSX.Element;
export {};
