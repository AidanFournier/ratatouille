import { FunctionInterpolation, Theme } from '@emotion/react';
export declare type PackageThemeValue = string | number | FunctionInterpolation<Record<string, unknown> & {
    theme: Theme;
}>;
export interface ThemeOnlyProps {
    theme: Theme;
}
declare module 'react' {
    interface HTMLAttributes<T> {
        'data-testid'?: string;
    }
}
