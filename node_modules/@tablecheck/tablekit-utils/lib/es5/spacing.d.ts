import { Theme } from '@emotion/react';
import { SerializedStyles, Interpolation } from '@emotion/serialize';
declare type SpacingDimension = string | number | undefined;
export declare type SpacingType = {
    bottom?: SpacingDimension;
    left?: SpacingDimension;
    right?: SpacingDimension;
    top?: SpacingDimension;
};
declare type SpacingArgument<Props extends {
    theme: Theme;
} = {
    theme: Theme;
}> = SpacingType | string | number | Interpolation<Props>;
export declare function parseSpacing(value?: SpacingDimension): string;
export declare function padding<Props extends {
    theme: Theme;
} = {
    theme: Theme;
}>(value: SpacingArgument<Props>): (props: Props) => SerializedStyles;
export declare function margin<Props extends {
    theme: Theme;
} = {
    theme: Theme;
}>(value: SpacingArgument<Props>): (props: Props) => SerializedStyles;
export {};
