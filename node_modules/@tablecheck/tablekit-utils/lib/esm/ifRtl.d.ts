import { Theme } from '@emotion/react';
import { CSSInterpolation, Interpolation } from '@emotion/serialize';
declare module '@emotion/react' {
    interface Theme {
        isRtl: boolean;
    }
}
export declare function ifRtl<ReturnValue extends CSSInterpolation, Props extends {
    theme: Theme;
} = {
    theme: Theme;
}>(truthyValue: ReturnValue | Interpolation<Props>, falsyValue: ReturnValue | Interpolation<Props>): (props: Props) => ReturnValue;
