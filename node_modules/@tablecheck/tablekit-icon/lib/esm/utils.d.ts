import { IconConfig, IconDefinition } from './types';
export declare const componentConfig: {
    icons?: Partial<IconConfig>;
};
export declare const config: (iconConfig: IconConfig) => void;
export declare const getIcon: (key: keyof IconConfig) => IconDefinition;
