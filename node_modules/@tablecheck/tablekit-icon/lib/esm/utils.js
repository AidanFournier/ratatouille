var componentConfig = {};

var config = function config(iconConfig) {
  componentConfig.icons = iconConfig;
};

var getIcon = function getIcon(key) {
  var icons = componentConfig.icons;
  if (!icons) throw new Error("'@tablecheck/tablekit-Icon: no configured icons, try adding one of the following as high in the component tree as possible `import '@tablecheck/tablekit-pro-icon-config';` or `import '@tablecheck/tablekit-free-icon-config';`'");
  var icon = icons[key];
  if (!icon) throw new Error("@tablecheck/tablekit-Icon: Icon \"".concat(key, "\" is not configured"));
  return icon;
};

export { componentConfig, config, getIcon };
